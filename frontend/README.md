# CryptoAI Intelligence Terminal Frontend

React + TypeScript + TailwindCSS terminal UI for the FastAPI forecasting backend.

```bash
npm install
npm run dev
npm run build
```

During development Vite proxies API calls to `http://localhost:8000`. In production, FastAPI serves `frontend/dist`.
